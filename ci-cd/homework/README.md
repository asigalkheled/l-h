# Run a Python app based on  Jenkins installed with Docker
## Steps
1. Create Python App
2. Wrap with Docker
3. Run Jenkins in Docker
4. Create Jenkins Pipeline
5. Run the Pipeline


This example builds a Docker image and runs the Python app automatically via a Jenkins pipeline
